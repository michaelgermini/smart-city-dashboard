# Configuration de l'environnement pour Docker
NODE_ENV=development

# Configuration de la base de données
DB_HOST=timescaledb
DB_PORT=5432
DB_NAME=smart_city
DB_USER=postgres
DB_PASSWORD=smartcity2024!

# Configuration du serveur backend
PORT=5000
FRONTEND_URL=http://localhost:3000

# Configuration des logs
LOG_LEVEL=info

# Configuration JWT (pour l'authentification)
JWT_SECRET=smart-city-dashboard-jwt-secret-key-2024-very-secure-and-long-key
JWT_EXPIRES_IN=24h

# Configuration des API externes
WEATHER_API_KEY=demo_weather_api_key
TPG_API_KEY=demo_tpg_api_key

# Configuration des seuils d'alerte
TRAFFIC_CONGESTION_THRESHOLD=80
PARKING_OCCUPANCY_THRESHOLD=90
TRANSPORT_DELAY_THRESHOLD=10

# Variables pour Docker
COMPOSE_PROJECT_NAME=smart-city-dashboard
